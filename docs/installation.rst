============
Installation
============

At the command line::

    $ easy_install django_bootstrap_calendar

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django_bootstrap_calendar
    $ pip install django_bootstrap_calendar